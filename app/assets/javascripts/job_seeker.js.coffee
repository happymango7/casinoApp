# $(document).on 'ready page:load', ->
#   $("body").off "shown.bs.tab", "#jobSeekerTab a"
#   $("#jobSeekerTab a").on "shown.bs.tab", (e) ->

#     $("input, textarea").each ->
#     	$("#" + $(this).attr("id") + "_preview").html $(this).val().replace(/\n/g, "<br />")
#     return

#   return
# return
